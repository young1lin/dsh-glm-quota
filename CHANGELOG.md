# 更新日志

记录用户可感知的变化；格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。
版本号见 `package.json`。

## [未发布]

### 改进

- 详情浮层标题显示套餐档位（`Coding Plan · Max 额度`），档位取宿主已投影的 `planLevel`。
  原先标题是写死的常量，只有折叠胶囊用到了套餐信息；档位为空时退回 `Coding Plan 额度`。
- 大于 0 的已用比例至少绘制 4% 周长的弧：1% 只有 3.6°，原本在凹槽上完全看不见，
  整枚环显示成坏掉的空圈。真正的 0% 仍然不画假弧，百分比数字保持真实值。
- 圆环凹槽改用新的 `--dsh-glm-track`（发丝边框色混入三级标签色加深），
  低用量时不再像一圈描边；22px 状态环改用单层内阴影，避免小尺寸下高光糊边。
- 倒计时省略为零的次级单位（`1d` 而非 `1d0h`），中心字号由两档改为三档
  （12 / 10.5 / 9px）：原来 `1h0m` 跳到 `59m` 时数字会突然变大，每小时抖一次。
- 详情行不再重复同一个数字：MCP 行的副标题去掉 `n% 已用`，比例交给圆环、
  精确次数交给右侧的 `11 / 4k`。

### 修复

- 手动刷新不再被正在进行的后台轮询吞掉。后台请求不带 `?refresh=1`，宿主可以
  用节流缓存应答；原先直接复用它会让刷新按钮转完一圈、报告成功，实际没有回源。
  现在强制刷新排队到后台轮询之后执行。
- 重置时间戳已过时，详情副标题显示 `即将刷新`，不再拼成 `即将刷新 后重置`。
- 概览提示中的百分比补上与其他读取处一致的兜底，宿主返回非数字时不再出现 `NaN%`。
- `999,999` 等计数先四舍五入再选单位，显示 `1M` 而不是 `1000k`。
- 详情浮层不再声明 `role="dialog"`：它从不接管焦点，宣告成对话框会把读屏用户
  困在一个永远收不到焦点的容器里；改为 `role="group"`，展开关系仍由触发按钮的
  `aria-expanded`/`aria-controls` 承担。

- 修复新版 DSH 主题（全局 `*,:before,:after{corner-shape:superellipse(1.5)}` 连续圆角）
  把 `border-radius:50%` 的额度圆环挤出正圆、渲染成方圆形并压住文字的问题：
  插件样式现在对自己声明的圆形/全圆角胶囊（`dshGlmRing`、内外盘、状态点、刷新按钮、
  折叠栏圆钮、42px 状态胶囊）显式回收 `corner-shape:round`，类选择器优先级高于通配规则，
  不与宿主全局样式冲突；低半径表面（详情浮层 16px 圆角）继续沿用宿主的连续圆角。

## [0.1.3] - 2026-09-02

### 改进

- 侧栏折叠（rail）态改为纯额度竖排：5h 与（存在时的）周额度 7d 各一枚小圆环，环弧即用量、环下自身重置倒计时，环旁不渲染数字（精确百分比在悬停 Tooltip）；不再显示混合最高已用窗口的单环，也不再含 MCP 次数与套餐名文案。无额度窗口时回退旧单环占位。
- 悬停 Tooltip 保留精确百分比与绝对重置时间；点击仍触发强制刷新；亮/暗主题五档配色同步覆盖新环。
- 宽侧栏胶囊的汇总百分比/圆环改为只取令牌额度窗口（5h、7d 等）的最大值：MCP 调用次数不再抬高外层读数（MCP 95% + 5h 1% 时外层显示 1%）；两个令牌窗口不一致时显示更接近上限的一方（7d 10% + 5h 5% → 10%）；仅剩 MCP 数据时回退显示 MCP。
- 令牌窗口判定收紧为 5h / 7d / 未知 `tok-*` 窗口：上游将来新增的非令牌类限额不再进入外层汇总与折叠环（仍保留在详情浮层）；百分比非数值时按 0 处理，杜绝 NaN%。
- 折叠环细节打磨：倒计时过期态紧凑显示 0m（避免中文折行）、悬停文案统一为"重置"、环项定宽 36px 且文本不可选中、reduced-motion 覆盖补齐。

## [0.1.2] - 2026-09-02

### 改进

- 重新设计侧栏额度入口：常驻区域缩减为 42px 状态胶囊，详情改为按需弹出的 Apple Control Center 风格浮层，不再持续挤占工作区列表。
- 使用紧凑圆环指标替代线性进度条；保留每个窗口的精确占用、MCP 次数、重置倒计时、手动刷新、陈旧状态和五档主题配色。
- 周额度严格按 API 的 `7d` 窗口条件渲染：V1/Max 无周额度时不留空位，V2 及以上返回后自动显示。
- 与 DSH 会话拖拽联动：拖到额度入口附近时继续滚动原生 workspace tree，避免 footer 截断底部自动滚动。
- 补充键盘、焦点、Esc/点外关闭、暗色主题与 reduced-motion 适配，并扩展 client smoke 覆盖。

## [0.1.1] - 2026-08-31

### 新增

- 包名改为 scoped：**`@young1lin/dsh-glm-quota`**（0.1.0 曾以非 scoped 名
  `dsh-glm-quota` 发布过一次，已废弃）。安装：
  `dsh plugin --profile web add @young1lin/dsh-glm-quota`。
- 声明 `dsh.bundle.patch`（随包发布 `cordis.patch.yml` 挂载行）：`dsh plugin add`
  一条命令完成安装与挂载，无需再手工编辑 `~/.dsh/cordis.patch.yml`。

## [0.1.0] - 2026-08-31

首个 npm 发布（包名 `dsh-glm-quota`；npm 上的 `glm-quota` 已被他人占用）。

### 新增

- 侧栏底部（Settings 上方）GLM Coding Plan 额度面板：多窗口进度条（5 小时 / 周限 / MCP）、
  五档配色（暗色主题适配）、折叠圆环态（最高已用窗口）、逐窗口重置倒计时、手动刷新。
- host 半场：会话事件驱动 + 单一节流阀（默认每 60s 至多一次上游请求），空闲零请求；
  状态文件跨进程权威；429 按 Retry-After 退避；凭据缺失不退避；凭据轮换即时重拉。
- `GET /glm-quota/state` 端点（`?refresh=1` 强制刷新）；`relevant` 门控 ——
  不用智谱或未配 key 的机器上面板自动隐藏、零上游请求。
- 冒烟测试：host 半场（窗口映射 / provider 门控 / 节流 / 退避 / 状态文件）与
  client 半场（handoff 格式 / 插槽注册 / 轮询 / SSR 渲染断言）。